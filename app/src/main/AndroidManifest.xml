<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ma1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpinnerActivity"></activity>
        <activity android:name=".RatingBarActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity
            android:name=".Second_R_Activity"
            android:label="Details"
            android:parentActivityName=".RecyclerViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".Audio_Activity" />
        <activity android:name=".video_View_activity" />
        <activity android:name=".Tic_Tac_Toe" />
        <activity android:name=".image_v_activity" />
        <activity android:name=".secondA_c" />
        <activity android:name=".secondA" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>